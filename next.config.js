/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['thehelpmatesfoundation.com', 'cdn.tailgrids.com', 'kutty.netlify.app']
  },
}

module.exports = nextConfig
